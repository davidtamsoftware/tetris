xdescribe("MatchService", () => {
    it("should return a list of active matches");
    it("should create a new match if one does not exist");
    it("should return an existing match if match id is found");
    it("should delete the match if all players in the match quit");
});
